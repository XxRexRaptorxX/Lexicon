{
  "schemaVersion": 1,
  "id": "${MOD_ID}",
  "version": "${MOD_VERSION}",
  "name": "${MOD_NAME}",
  "description": "${MOD_DESCRIPTION}",
  "authors": [
    {
      "name": "${MOD_AUTHOR}",
      "contact": {
        "homepage": "${MOD_HOMEPAGE}",
        "issues": "${MOD_ISSUES}",
        "sources": "${MOD_SOURCE}"
      }
    }
  ],
  "contact": {
    "homepage": "${MOD_HOMEPAGE}",
    "issues": "${MOD_ISSUES}",
    "sources": "${MOD_SOURCE}"
  },
  "license": "${MOD_LICENSE}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "${MOD_GROUP}.fabric.${MOD_NAME}Fabric"
    ]
  },
  "mixins": [],
  "depends": {
    "minecraft": "~${libs.versions.minecraft.get()}",
    "java": ">=${libs.versions.java.get()}",
    "fabric": "*"
  }
}
